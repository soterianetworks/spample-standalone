import {Base} from '../common/base.model';

export class Machine extends Base {
    code: string;
    name: string;
    model: string;
    brand: string;
    vendor: string;
    description: string;
}
